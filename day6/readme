day6 计算器作业

模拟计算器计算过程
输入一个表达式， 如 1 + (4/-2-(-4/2+4-2*-1))
                   8*12 + (6-(15-6*-2-(-2/1*3-1))/77+2)*(9-7)+8
不使用eval等函数，计算出结果


设计思路
1）先使用正则匹配得到表达式最内层括号内容s
   将原始表达式分隔成 's_before + s + s_back'
2）计算s 得到结果r
3) 将r替换原始表达式内的s,得到一个新的表达式s
                     's_before + r + s_back'
4）循环匹配括号并计算 直到替换掉所有括号 直接计算表达式


计算模拟过程
# s1 = 1 + (4/-2-(-4/2+4-2*-1))
# s2 = 1 + (4/-2-(-2+4-2*-1))
# s3 = 1 + (4/-2-(-2+4--2))
# s4 = 1 + (4/-2-(-2+4+2))
# s5 = 1 + (4/-2-4)
# s6 = 1 + -6
# s7 = -5


所需知识
1）重要模块 re 运用
   re.match()
   re.search()
   re.split()
   group()
   groups()
   groupdict()

2) 正则表达式 . *  [] \d + \
   re.split('(li)', origin, 1) 分组匹配

3) 列表操作

4）逻辑判断